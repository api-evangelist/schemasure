# SchemaSure

Structured data extraction API that converts unstructured text or HTML into JSON guaranteed to validate against a caller-supplied JSON Schema, or returns a typed error at no charge. Access is gated by x402 pay-per-call micropayments (USDC on Base), with no accounts or API keys.

- **Provider:** https://schemasure.com
- **Documentation:** https://schemasure.com/llms.txt
- **Base URL:** https://schemasure.com
- **Tags:** structured-data-extraction, text-to-JSON, JSON-Schema, document-parsing, data-cleaning, LLM-tooling, AI-agents, x402-micropayments, agent-native, A2A, MCP
- **Public API:** yes
- **Agent-native (MCP / llms.txt / skills):** yes
- **Confidence:** 95/100

## APIs

- **SchemaSure Structured Extraction API** — REST API (OpenAPI 3.1) exposing POST /v2/extract (x402 V2 paid), legacy POST /extract, GET /health, and GET /metrics. Converts text/HTML into schema-valid JSON. Includes llms.txt integration guide and an A2A agent card. (`https://schemasure.com`)

> Confirmed public REST API (OpenAPI 3.1 at /openapi.json), llms.txt, and an A2A agent card (/.well-known/agent.json) with skill extract_to_schema. The MCP server is local stdio only (run via the github.com/noamjose/schemasure repo) with no hosted/remote endpoint, so it is NOT cataloged as an MCP property. No GraphQL, AsyncAPI, or Postman collection published. Auth is via x402 micropayments (USDC on Base mainnet, $0.01 per successful call, charge-only-on-success); no keys or accounts. Additional well-knowns: /.well-known/x402, /.well-known/x402-v1, /.well-known/pricing.json, /.well-known/benchmarks.json, /.well-known/security.txt. Not surfaced by general web search; all confirmation direct from provider endpoints.

_Generated by apis.io from a community submission._